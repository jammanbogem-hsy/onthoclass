{
  "name": "jamenglish-functions",
  "version": "1.0.0",
  "description": "잼클래스 온톨로지 추출 Cloud Functions",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.96.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "private": true
}
